# Git-first
My first git exploration
Author - Aniket (change)

git clone url at local machine

git has 4 status - untracked, modified, staged (file's ready to commit), unmodified

git 2 important command - add & commit (only at local screen)

how to send local changes to repo - git push origin main
